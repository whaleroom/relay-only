# ── Required ──
FEED_KEY=<64-char hex from seed node operator>

# ── Relay settings ──
PORT=4488
BIND_ADDR=0.0.0.0
DATA_DIR=./data
BRIDGE_ENABLED=false

# ── Token gate (optional, enabled by default) ──
TOKEN_GATE_ENABLED=true
RPC_URL=https://ethereum-rpc.publicnode.com,https://1rpc.io/eth
